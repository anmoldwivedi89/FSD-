// variable hositing - all the function declarations go on top
console.log("Hi fron node....")

// Variable types and data types
const userId = 4042;
let points = 150;
var status = "active";

// Updating values
points = 200;

// Complex data types
const userProfile = {
  name: "Alex",
  isAdmin: true
};

const tags = ["premium", "verified"];

// Output results
console.log(userId);
console.log(points);
console.log(userProfile.name);
console.log(tags[0]);
